            Put( file, Format( person.identifier_household ));
            Put( file, Format( person.identifier_person ));
            Put( file, Format( person.identifier_partner ));
            Put( file, Format( person.identifier_mother ));
            Put( file, Format( person.identifier_father ));
            Put( file, Format( person.demographic_country ));
            Put( file, Format( person.demographic_weight ));
            Put( file, Format( person.demographic_age ));
            Put( file, Format( Value( person.demographic_gender ) ));
            Put( file, Format( Value( person.demographic_marital_status ) ));
            Put( file, Format( Value( person.demographic_education_current_status ) ));
            Put( file, Format( person.labour_market_economic_status ));
            Put( file, Format( Boolean'Image( person.labour_market_civil_servant ) ));
            Put( file, Format( Value( person.demographic_consensual_union ) ));
            Put( file, Format( Value( person.demographic_region_nuts_level_1 ) ));
            Put( file, Format( person.tax_income_tax_tax_allowance_tax_credit_simulated ));
            Put( file, Format( person.tax_income_tax_tax_credit_replacement_income_simulated ));
            Put( file, Format( person.tax_income_tax_tax_credit_child_simulated ));
            Put( file, Format( person.tax_income_tax_tax_credit_low_pay_or_income_simulated ));
            Put( file, Format( person.tax_income_tax_schedule_simulated ));
            Put( file, Format( person.income_disposable ));
            Put( file, Format( person.tax_income_tax_national_government_simulated ));
            Put( file, Format( Value( person.assets_main_residence_tenure ) ));
            Put( file, Format( Value( person.labour_market_occupation_isco_1_digit ) ));
            Put( file, Format( person.income_employment ));
            Put( file, Format( person.income_investment ));
            Put( file, Format( person.income_other ));
            Put( file, Format( person.income_property ));
            Put( file, Format( person.income_private_pension ));
            Put( file, Format( person.income_private_transfers ));
            Put( file, Format( person.income_self_employment ));
            Put( file, Format( person.expenditure_maintenance_payment ));
            Put( file, Format( person.pension_old_age ));
            Put( file, Format( person.pension_survivors ));
            Put( file, Format( person.pension_disability ));
            Put( file, Format( person.pension_health ));
            Put( file, Format( person.benefit_early_retirement ));
            Put( file, Format( person.benefit_social_assistance_simulated ));
            Put( file, Format( person.benefit_social_assistance_old_age_simulated ));
            Put( file, Format( person.benefit_education ));
            Put( file, Format( person.benefit_housing ));
            Put( file, Format( person.benefit_child_simulated ));
            Put( file, Format( person.benefit_child_birth_or_adoption_simulated ));
            Put( file, Format( person.benefit_unemployment ));
            Put( file, Format( person.benefit_unemployment_simulated ));
            Put( file, Format( person.benefit_family_parental_leave ));
            Put( file, Format( person.benefit_maternity ));
            Put( file, Format( person.tax_income_tax_capital_taxation_simulated ));
            Put( file, Format( person.tax_income_tax_municipal_government_simulated ));
            Put( file, Format( person.tax_sic_employee_simulated ));
            Put( file, Format( person.tax_sic_pensioner_simulated ));
            Put( file, Format( person.tax_sic_disabled_simulated ));
            Put( file, Format( person.tax_sic_employee_reduction_simulated ));
            Put( file, Format( person.tax_sic_employee_spouse_simulated ));
            Put( file, Format( person.tax_sic_self_employed_simulated ));
            Put( file, Format( person.standard_disposable_income ));
            Put( file, Format( person.persons_monthly_original_income ));
            Put( file, Format( person.sum_of_all_benefits_received_by_the_person ));
            Put( file, Format( person.sum_of_all_taxes_paid_by_the_person ));
            Put( file, Format( person.employee_social_insurance_contributions_paid_by_the_person ));
            Put( file, Format( person.employer_social_insurance_contributions_paid_for_the_person_not_included_in_ils_dispy ));
            Put( file, Format( person.self_employed_social_insurance_contributions_paid_by_the_person ));
            Put( file, Format( person.pension ));
            Put( file, Format( person.taxable_income ));
            Put( file, Format( person.taxable_income_something_mq ));
            Put( file, Format( person.bennt ));
            Put( file, Format( person.ils_benmt ));
            Put( file, Format( person.bensim ));
            Put( file, Format( person.taxsim ));
            Put( file, Format( person.ils_tinty ));
            Put( file, Format( person.tu_family_be_headid ));
            Put( file, Format( person.head_of_household_indentifier ));
